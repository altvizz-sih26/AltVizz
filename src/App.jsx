import { Routes, Route, Link, useNavigate } from 'react-router-dom';
import { useEffect, useRef, useState } from 'react';
import Layout from './components/Layout';
import { DEMO_CONFIG } from './config/demoConfig';
import { clearDemoJob, downloadResult, getJob, getResults, startProcessing, uploadImage } from './services/pipelineService';

const stages=['Image Ingestion','Depth Estimation','Terrain Classification','Reference Elevation','Height Calibration','DSM Generation','3D Reconstruction'];
const Arrow=()=> <span className="arrow">→</span>;
function Home(){return <Layout><section className="hero"><p className="eyebrow">SATELLITE TERRAIN INTELLIGENCE</p><h1>From satellite imagery<br/>to <i>3D terrain.</i></h1><p className="lede">Transform a single optical image into elevation-aware terrain and an interactive 3D model.</p><div className="actions"><Link className="button primary" to="/upload">Start Reconstruction <Arrow/></Link><a className="button secondary" href="#workflow">Explore Workflow</a></div></section><section id="workflow" className="section"><p className="eyebrow">THE RECONSTRUCTION PATH</p><h2>Terrain intelligence, layer by layer.</h2><div className="workflow">{['Satellite Image','Depth Estimation','Terrain Analysis','Elevation Calibration','DSM','3D Terrain'].map((x,i)=><div className="flow" key={x}><div className="flow-num">0{i+1}</div><span>{x}</span>{i<5&&<Arrow/>}</div>)}</div></section><section className="split section"><div><p className="eyebrow">FROM 2D TO TOPOGRAPHY</p><h2>Seeing height where imagery sees colour.</h2></div><p>DepthWizard brings together monocular depth estimation, terrain-aware calibration and reference elevation data to create a meaningful 3D terrain interpretation—without overstating certainty.</p></section></Layout>}
function Upload(){const nav=useNavigate(),[file,setFile]=useState(null),[error,setError]=useState(''),[drag,setDrag]=useState(false);const pick=f=>{if(!f)return;const ext=f.name.split('.').pop().toLowerCase();if(!DEMO_CONFIG.acceptedExtensions.includes(ext)){setError('Please select a PNG, JPG, JPEG, TIFF, or TIF image.');return}setError('');setFile(f)};const go=async()=>{await uploadImage(file);await startProcessing();nav('/processing')};return <Layout><section className="page narrow"><p className="eyebrow">STEP 01 / INPUT</p><h1>Upload satellite imagery.</h1><p className="lede">Upload a single optical satellite or remote-sensing image to begin the demo reconstruction.</p><label className={'dropzone '+(drag?'dragging':'')} onDragOver={e=>{e.preventDefault();setDrag(true)}} onDragLeave={()=>setDrag(false)} onDrop={e=>{e.preventDefault();setDrag(false);pick(e.dataTransfer.files[0])}}><input type="file" accept=".png,.jpg,.jpeg,.tiff,.tif" onChange={e=>pick(e.target.files[0])}/><div className="upload-icon">⇧</div><b>Drop your image here</b><span>or <u>browse files</u></span><small>PNG, JPG, JPEG, TIFF or TIF · one image only</small></label>{error&&<p className="error">{error}</p>}{file&&<div className="file-card">{file.type.startsWith('image/')&&!/tiff/i.test(file.name)&&<img src={URL.createObjectURL(file)} alt="Selected satellite preview"/>}<div><p className="eyebrow">SELECTED INPUT</p><b>{file.name}</b><small>{file.type || 'Image file'} · {(file.size/1024/1024).toFixed(2)} MB</small>{/tiff/i.test(file.name)&&<small>TIFF selected — browser preview unavailable; it will be preserved for processing.</small>}</div><button onClick={()=>setFile(null)} className="text-button">Remove</button></div>}<div className="actions right"><button disabled={!file} onClick={go} className="button primary">Start Processing <Arrow/></button></div></section></Layout>}
function Processing(){const nav=useNavigate(),job=getJob(),[active,setActive]=useState(0),[progress,setProgress]=useState(2);useEffect(()=>{if(!job){nav('/upload');return}let tick=0;const timer=setInterval(()=>{tick++;setProgress(p=>Math.min(100,p+2));setActive(Math.min(6,Math.floor(tick/12)));if(tick>=49){clearInterval(timer);setTimeout(()=>nav('/results'),650)}},100);return()=>clearInterval(timer)},[]);return <Layout><section className="page narrow processing"><p className="eyebrow">DEMO PIPELINE · {job?.fileName}</p><h1>{progress===100?'Reconstruction Complete':'Reconstructing terrain…'}</h1><div className="progress"><i style={{width:progress+'%'}}/></div><div className="progress-label"><span>Overall progress</span><b>{progress}%</b></div><div className="stage-list">{stages.map((s,i)=><div className={'stage '+(i<active?'done':i===active?'active':'')} key={s}><span className="stage-index">{String(i+1).padStart(2,'0')}</span><b>{s}</b><em>{i<active?'Completed':i===active?'Processing':'Waiting'}</em><span className="status">{i<active?'✓':i===active?'◌':'—'}</span></div>)}</div><p className="demo-note">Demo mode simulates the planned pipeline locally; no backend calls are being made.</p></section></Layout>}
function TerrainArt(){return <div className="terrain-art" aria-label="Demo DSM elevation visualization"><div className="contours"/><span>HIGH · 842m</span><small>LOW · 215m</small></div>}
function Results(){const nav=useNavigate(),[modal,setModal]=useState(''),[data,setData]=useState(null);useEffect(()=>{getResults().then(setData)},[]);const fresh=()=>{clearDemoJob();nav('/upload')};return <Layout><section className="page"><p className="eyebrow">OUTPUT / DEMO MODE</p><h1>Reconstruction Complete.</h1><p className="lede">Your terrain package is ready to inspect and explore.</p><div className="input-line"><span>INPUT IMAGE</span><b>{data?.fileName || DEMO_CONFIG.defaultFileName}</b><i>{data?.fileType || 'image/tiff'}</i></div><div className="results-grid"><article className="result-card"><TerrainArt/><div className="card-copy"><p className="eyebrow">ELEVATION / DSM</p><h2>Digital Surface Model</h2><p>Terrain-relative elevation visualisation generated for this demo output.</p><button className="button secondary" onClick={()=>setModal('dsm')}>View DSM</button></div></article><article className="result-card"><div className="model-preview"><span>◒</span><div>GLB<br/>TERRAIN</div></div><div className="card-copy"><p className="eyebrow">3D TERRAIN MODEL</p><h2>Terrain mesh</h2><p>{DEMO_CONFIG.demoGlbName} · GLB format</p><div className="button-row"><button className="button secondary" onClick={()=>setModal('glb')}>View GLB Output</button><button className="icon-button" title="Download GLB" onClick={downloadResult}>↓</button></div></div></article><article className="summary-card"><p className="eyebrow">PROCESSING SUMMARY</p><dl><dt>Input format</dt><dd>{data?.fileType || 'image/tiff'}</dd><dt>Output format</dt><dd>DSM + GLB</dd><dt>Reconstruction</dt><dd className="success">Complete</dd><dt>Mode</dt><dd>Local prototype demo</dd></dl><a className="button primary full" href="/viewer" target="_blank" rel="noopener noreferrer">View 3D Model <Arrow/></a></article></div><button className="text-button back" onClick={fresh}>← Upload New Image</button></section>{modal&&<div className="modal-backdrop" onMouseDown={()=>setModal('')}><div className="modal" onMouseDown={e=>e.stopPropagation()}><button className="close" onClick={()=>setModal('')}>×</button>{modal==='dsm'?<><TerrainArt/><h2>Digital Surface Model</h2><p>Demo elevation visualisation for the selected input image.</p></>:<><div className="model-preview big">GLB</div><h2>GLB Terrain Output</h2><p>This locally generated demo GLB is ready for download or exploration in the interactive viewer.</p><div className="actions"><button className="button secondary" onClick={downloadResult}>Download GLB</button><button className="button primary" onClick={()=>nav('/viewer')}>Open Viewer</button></div></>}</div></div>}</Layout>}
function ViewerCanvas({top,reset,fly}){const ref=useRef();useEffect(()=>{const c=ref.current,ctx=c.getContext('2d');let angle=top?0:.52,zoom=1,drag=false,last;const draw=()=>{const w=c.clientWidth,h=c.clientHeight;c.width=w*devicePixelRatio;c.height=h*devicePixelRatio;ctx.scale(devicePixelRatio,devicePixelRatio);ctx.clearRect(0,0,w,h);ctx.fillStyle='#071228';ctx.fillRect(0,0,w,h);const cols=37,rows=29,pts=[];for(let y=0;y<rows;y++){pts[y]=[];for(let x=0;x<cols;x++){let xx=(x-cols/2)*15,yy=(y-rows/2)*15,z=(Math.sin(x*.35)*28+Math.cos(y*.32)*23+Math.sin((x+y)*.19)*18)*zoom;let rx=xx*Math.cos(angle)-yy*Math.sin(angle),ry=xx*Math.sin(angle)+yy*Math.cos(angle);pts[y][x]=[w/2+rx*zoom,h/2+ry*.4-z*.8]}}for(let y=0;y<rows-1;y++)for(let x=0;x<cols-1;x++){ctx.beginPath();ctx.moveTo(...pts[y][x]);ctx.lineTo(...pts[y][x+1]);ctx.lineTo(...pts[y+1][x+1]);ctx.lineTo(...pts[y+1][x]);ctx.closePath();ctx.fillStyle=`hsla(${205+(y/rows)*45},65%,${22+(Math.sin(x*.4+y*.3)+1)*9}%,.95)`;ctx.fill();ctx.strokeStyle='rgba(176,209,255,.15)';ctx.stroke()}if(fly)angle+=.006;requestAnimationFrame(draw)};draw();const down=e=>{drag=true;last=e.clientX},move=e=>{if(drag){angle+=(e.clientX-last)*.01;last=e.clientX}},up=()=>drag=false,wheel=e=>{zoom=Math.max(.55,Math.min(1.6,zoom-e.deltaY*.001))};c.addEventListener('pointerdown',down);c.addEventListener('pointermove',move);addEventListener('pointerup',up);c.addEventListener('wheel',wheel);return()=>{c.removeEventListener('pointerdown',down);c.removeEventListener('pointermove',move);removeEventListener('pointerup',up);c.removeEventListener('wheel',wheel)}},[top,reset,fly]);return <canvas ref={ref} className="terrain-canvas"/>}
function Viewer(){const nav=useNavigate(),[top,setTop]=useState(false),[reset,setReset]=useState(0),[fly,setFly]=useState(false),wrap=useRef();const full=()=>wrap.current.requestFullscreen?.();return <Layout><section className="viewer-page"><div className="viewer-title"><div><p className="eyebrow">INTERACTIVE TERRAIN VIEWER</p><h1>Demo terrain model</h1><span className="online">● GLB ready · local demo asset</span></div><button className="text-button" onClick={()=>nav('/results')}>← Back to results</button></div><div className="viewer-shell" ref={wrap}><ViewerCanvas top={top} reset={reset} fly={fly}/><div className="viewer-hint">Drag to orbit · scroll to zoom</div><div className="viewer-controls"><button onClick={()=>setReset(x=>x+1)}>Reset view</button><button onClick={()=>setTop(!top)}>{top?'Perspective':'Top view'}</button><button className={fly?'selected':''} onClick={()=>setFly(!fly)}>Flythrough</button><button onClick={full}>Fullscreen</button><button onClick={downloadResult}>↓ Download GLB</button></div></div></section></Layout>}
export default function App(){return <Routes><Route path="/" element={<Home/>}/><Route path="/upload" element={<Upload/>}/><Route path="/processing" element={<Processing/>}/><Route path="/results" element={<Results/>}/><Route path="/viewer" element={<Viewer/>}/><Route path="*" element={<Home/>}/></Routes>}
